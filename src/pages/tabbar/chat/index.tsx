import styles from './index.module.scss';

const Index: React.FC = () => {
  return (
    <div className={styles.page}>
      <div className={styles.col}>chat</div>
    </div>
  );
};

export default Index;
